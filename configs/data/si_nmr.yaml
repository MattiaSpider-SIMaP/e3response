_target_: e3response.data.si_nmr.SiNmrDataModule
r_max: ${r_max}
batch_size: 20 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
train_val_test_split: [0.80, 0.10, 0.10]
# limit: 50
stratify: true # true: stratified split by Qn signature; false: plain random split
